# Publish Configuration for `html_to_markdown_ffi`

<!-- Auto-generated by publish-manager skill on 2026-08-05. Edit if the workflow changes. -->

## Package Manager

- **Type**: `pub.dev`
- **Packages**: `html_to_markdown_ffi` (single package)
- **Public packages**: `html_to_markdown_ffi` → https://pub.dev/packages/html_to_markdown_ffi
- **Private packages**: none

## Scripts

- **Pre-publish**: none — bump `version:` in `pubspec.yaml` and the `CHANGELOG.md` manually
- **Publish**: `dart pub publish` — publishes to pub.dev (interactive; answer `y` to confirm)
- **Post-publish**: none — merge `publish-<version>` into `master` via GitHub PR
- **Restore dev**: none

## Workflow

1. Update `CHANGELOG.md` (add `## <version> - YYYY-MM-DD` entry)
2. Bump `version:` in `pubspec.yaml`
3. `git checkout -b publish-<version>`
4. Commit changes with message `Prepare for publishing version <version>`
5. Push branch and open a PR
6. `dart pub publish` (dry-run first: `dart pub publish --dry-run`)
7. `git tag v<version>` and `git push origin v<version>`

## Notes

- Git tags use a `v` prefix (e.g. `v1.1.0`).
- Prebuilt native binaries are shipped in `native/` and are included in the
  published archive (verified via dry-run). The GitHub release download remains
  a fallback for platforms without bundled artifacts.
- `pubspec.lock` is gitignored but committed in the repo; pub prints a
  non-blocking warning about it on publish. Known/fine.
- Validated with `dart analyze` (infos only in `lib/`) and `dart test` (all pass)
  before publishing.
